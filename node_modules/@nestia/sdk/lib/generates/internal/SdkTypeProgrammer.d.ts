import ts from "typescript";
import { Metadata } from "typia/lib/schemas/metadata/Metadata";
import { MetadataObject } from "typia/lib/schemas/metadata/MetadataObject";
import { INestiaConfig } from "../../INestiaConfig";
import { ImportDictionary } from "./ImportDictionary";
export declare namespace SdkTypeProgrammer {
    const write: (config: INestiaConfig) => (importer: ImportDictionary) => (meta: Metadata, parentEscaped?: boolean) => ts.TypeNode;
    const write_object: (config: INestiaConfig) => (importer: ImportDictionary) => (object: MetadataObject) => ts.TypeNode;
}
