import ts from "typescript";
import { INestiaConfig } from "../../INestiaConfig";
import { IRoute } from "../../structures/IRoute";
import { ImportDictionary } from "./ImportDictionary";
export declare namespace SdkRouteProgrammer {
    const generate: (checker: ts.TypeChecker) => (config: INestiaConfig) => (importer: ImportDictionary) => (route: IRoute) => ts.Statement[];
}
