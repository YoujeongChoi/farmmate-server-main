import ts from "typescript";
import { INestiaConfig } from "../../INestiaConfig";
import { IRoute } from "../../structures/IRoute";
import { ImportDictionary } from "./ImportDictionary";
export declare namespace SdkInterfaceProgrammer {
    interface IModule {
        name: string;
        children: Map<string, IModule>;
        programmer: null | ((importer: ImportDictionary) => ts.TypeAliasDeclaration);
    }
    const write: (checker: ts.TypeChecker) => (config: INestiaConfig) => (routes: IRoute[]) => Map<string, IModule>;
}
