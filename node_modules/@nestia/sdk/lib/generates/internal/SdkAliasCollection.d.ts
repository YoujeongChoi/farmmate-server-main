import ts from "typescript";
import { INestiaConfig } from "../../INestiaConfig";
import { IRoute } from "../../structures/IRoute";
import { ImportDictionary } from "./ImportDictionary";
export declare namespace SdkAliasCollection {
    const name: (config: INestiaConfig) => (importer: ImportDictionary) => (p: IRoute.IParameter | IRoute.IOutput) => ts.TypeNode;
    const headers: (config: INestiaConfig) => (importer: ImportDictionary) => (param: IRoute.IParameter) => ts.TypeNode;
    const query: (config: INestiaConfig) => (importer: ImportDictionary) => (param: IRoute.IParameter) => ts.TypeNode;
    const input: (config: INestiaConfig) => (importer: ImportDictionary) => (param: IRoute.IParameter) => ts.TypeNode;
    const output: (checker: ts.TypeChecker) => (config: INestiaConfig) => (importer: ImportDictionary) => (route: IRoute) => ts.TypeNode;
    const responseBody: (checker: ts.TypeChecker) => (config: INestiaConfig) => (importer: ImportDictionary) => (route: IRoute) => ts.TypeNode;
}
